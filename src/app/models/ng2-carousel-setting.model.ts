import { Ng2AnimationType } from './ng2-carousel-animation-type.model';

export interface Ng2CarouselSetting {
  autoPlay: boolean,
  showArrow: boolean,
  showDots: boolean,
  holdTime: number,
  animation: Ng2AnimationType
}